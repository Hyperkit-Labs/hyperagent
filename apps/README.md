# Applications

This directory contains the main applications for the HyperAgent platform.

## Structure

- **web/**: Next.js + React frontend application
- **api-gateway/**: API gateway (auth, routing, x402)

## Development

Each application has its own `package.json` or `requirements.txt` and can be developed independently.

