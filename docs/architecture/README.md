# Architecture documentation

This section holds **human-readable architecture** and **diagrams as code** for HyperAgent.

| Doc | Purpose |
|-----|---------|
| [System context](system-context.md) | High-level diagram (Mermaid): clients, gateway, orchestrator, data plane |
| [Network architecture](networks.md) | Chain registry, x402, execution defaults |

Deeper repository mapping lives in the [agent operating model](../agent-operating-model/architecture-map.md).

## Diagrams as code

- Prefer **Mermaid** inside Markdown (`.md`) so MkDocs and GitHub both render diagrams.
- Optional: store standalone Mermaid in `.mmd` files only when shared across multiple pages or generated by tooling.
