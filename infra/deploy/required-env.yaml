# Required environment keys per profile for scripts/deploy/validate_env.py
#
# example-keys: every listed key must appear in .env.example (documentation gate for PRs).
# strict: every listed key must have a non-empty value in the process environment and/or --env-file.
#
# Adjust lists as services evolve; keep profiles aligned with docs/storage-policy and production.mdc.

profiles:
  ci:
    required:
      - SUPABASE_URL
      - DATABASE_URL
      - REDIS_URL
      - UPSTASH_REDIS_REST_URL
      - UPSTASH_REDIS_REST_TOKEN
      - ORCHESTRATOR_URL
      - NEXT_PUBLIC_API_URL
      - NEXT_PUBLIC_THIRDWEB_CLIENT_ID
      - NEXT_PUBLIC_ENV
      - AUTH_JWT_SECRET
      - JWT_SECRET_KEY
      - AGENT_SESSION_PAYLOAD_KEY
      - INTERNAL_SERVICE_TOKEN
      - LLM_KEY_ENCRYPTION_KEY
      - LLM_KEY_KMS_KEY_ARN

  staging:
    required:
      - SUPABASE_URL
      - DATABASE_URL
      - REDIS_URL
      - UPSTASH_REDIS_REST_URL
      - UPSTASH_REDIS_REST_TOKEN
      - ORCHESTRATOR_URL
      - AGENT_RUNTIME_URL
      - NEXT_PUBLIC_API_URL
      - NEXT_PUBLIC_THIRDWEB_CLIENT_ID
      - NEXT_PUBLIC_ENV
      - NEXT_PUBLIC_SUPABASE_URL
      - NEXT_PUBLIC_SUPABASE_ANON_KEY
      - AUTH_JWT_SECRET
      - JWT_SECRET_KEY
      - AGENT_SESSION_PAYLOAD_KEY
      - INTERNAL_SERVICE_TOKEN
      - LLM_KEY_ENCRYPTION_KEY
      - LLM_KEY_KMS_KEY_ARN
      - PINATA_JWT
      - THIRDWEB_SECRET_KEY

  production:
    required:
      - SUPABASE_URL
      - DATABASE_URL
      - REDIS_URL
      - UPSTASH_REDIS_REST_URL
      - UPSTASH_REDIS_REST_TOKEN
      - ORCHESTRATOR_URL
      - AGENT_RUNTIME_URL
      - NEXT_PUBLIC_API_URL
      - NEXT_PUBLIC_THIRDWEB_CLIENT_ID
      - NEXT_PUBLIC_ENV
      - NEXT_PUBLIC_SUPABASE_URL
      - NEXT_PUBLIC_SUPABASE_ANON_KEY
      - AUTH_JWT_SECRET
      - JWT_SECRET_KEY
      - AGENT_SESSION_PAYLOAD_KEY
      - INTERNAL_SERVICE_TOKEN
      - LLM_KEY_ENCRYPTION_KEY
      - LLM_KEY_KMS_KEY_ARN
      - PINATA_JWT
      - LIGHTHOUSE_API_KEY
      - THIRDWEB_SECRET_KEY
      - IDENTITY_HMAC_SECRET
