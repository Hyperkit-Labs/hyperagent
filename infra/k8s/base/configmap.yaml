apiVersion: v1
kind: ConfigMap
metadata:
  name: hyperagent-config
  labels:
    app: hyperagent
data:
  LOG_LEVEL: "INFO"
  API_VERSION: "v1"
  CORS_ORIGINS: "*"
  RATE_LIMIT_ENABLED: "true"
  RATE_LIMIT_REQUESTS: "100"
  RATE_LIMIT_WINDOW: "60"

