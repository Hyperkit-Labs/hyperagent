apiVersion: v1
kind: Service
metadata:
  name: hyperagent-api
  labels:
    app: hyperagent
    component: api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: hyperagent
    component: api

