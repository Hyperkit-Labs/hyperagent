apiVersion: apps/v1
kind: Deployment
metadata:
  name: hyperagent-api
spec:
  template:
    spec:
      containers:
      - name: api
        env:
        - name: ENVIRONMENT
          value: production
        - name: DEBUG
          value: "false"

