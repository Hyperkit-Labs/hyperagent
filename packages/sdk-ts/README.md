# SDK TypeScript

Status: explicit roadmap placeholder.

This workspace is reserved for a future public TypeScript SDK, but the SDK does not exist yet in this repo. There is no supported runtime API here today, and production code should not depend on this package.

Current guidance:
- use active app/service packages for runtime behavior
- use `@hyperagent/api-contracts` and `@hyperagent/core-types` for current shared contract surfaces
- only promote this workspace once real source, tests, and ownership exist
