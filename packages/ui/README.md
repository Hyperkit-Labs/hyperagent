# UI

Status: explicit roadmap placeholder.

This workspace is reserved for a future shared React/Tailwind component library. It does not currently provide a supported production component surface, and no application code in this repo imports it today. There are no supported runtime exports yet.

Current guidance:
- keep Studio components in `apps/studio` until a real shared component contract exists
- do not add production dependencies on this package until it has real source, tests, and ownership
