[mypy]
python_version = 3.11
ignore_missing_imports = True
# Only typecheck orchestrator sources; do not analyze arbitrary site-packages (avoids
# mypy crashes in unrelated installed libs on developer machines).
follow_imports = skip
# FastAPI routes use `request: Request = None`; allow implicit Optional until migrated.
no_implicit_optional = False
show_error_codes = True
